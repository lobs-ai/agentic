{
    "name": "agentic",
    "version": "0.1.0",
    "private": true,
    "description": "A modular toolkit for building AI agents — extracted from lobs-core",
    "license": "MIT",
    "scripts": {
        "build": "for p in packages/llm packages/tools packages/runner packages/agentic packages/discord; do (cd $p && npx tsc); done",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "vitest": "^4.1.4"
    }
}
